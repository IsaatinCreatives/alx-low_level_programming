#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: 0
 */
int main(void)
{
	int numn;

	for (num = 0; num < 10; num++)
		printf("%d", num);

	printf('\n');

	return (0);
}
